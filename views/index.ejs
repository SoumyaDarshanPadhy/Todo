<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/styles.css">
    <title> <%= title %></title>
</head>
<body>
    

    <div class="box" id="heading">
        <h1><%= day %></h1>
    </div>

    <div class="box">
        <div id="tasks">

            <% newItem.forEach(tasks => { %>
                <form action="/delete" method="post">
                    <div class="item">
                        <input type="checkbox" name="deleteItem" value=<%= tasks._id %> onchange="this.form.submit()">
                        <p> <%= tasks.task %></p>
                    </div>
                </form>
            <% }) %>

        </div>

        <form class="addItem" action="/" method="post">
            <input type="text" autofocus name="task" autocomplete="off" placeholder="Add a new Task" required>
            <button type="submit">+</button>
        </form>

    </div>
    

</body>
</html>